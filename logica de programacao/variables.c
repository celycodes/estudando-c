#include <stdio.h>
// trabalahndo com variaveis em c

int main(void) {
  char letra = 'a';
  int num = 10;
  float n = 5.25;

  printf("%c \n", letra);
  printf("%d \n", letra); // valor numerico de 'a' dentro da tabela ASCII
  printf("Num: %d, num+1: %d \n", num, num+1);
  printf("%f \n", n);

  return 0;
}